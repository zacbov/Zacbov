sounddevice==0.4.7
numpy==1.26.4
PyYAML==6.0.1
Flask==3.0.3
Flask-SocketIO==5.3.6
obsws-python==1.7.0
python-socketio==5.11.2
mido==1.3.2
python-rtmidi==1.5.8

# --- Optionnel : nécessaire uniquement si detection.vad_engine = "silero" ---
# Sans ces deux paquets, le système fonctionne quand même : il détecte
# automatiquement leur absence et repasse en détection RMS (CPU, sans GPU).
# Pour l'accélération GPU NVIDIA, installe la version CUDA de torch :
# https://pytorch.org/get-started/locally/
# torch>=2.2

